[project]
name = "resume-agent-mvp"
version = "0.1.0"
description = "A minimal AI agent demo for resume and job-description matching."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=4.44.0",
    "openai>=1.40.0",
    "python-dotenv>=1.0.1",
    "pymupdf>=1.24.9",
]

[tool.uv]
package = false
